# Story 1.1: Add CLI Command Parsing for Restart

## Description
Implement the CLI parsing logic for the `restart` command and wire it up to the existing .NET CLI structure using Spectre.Console.

## Acceptance Criteria
- [x] The .NET CLI registers and accepts the `restart` command/verb.
- [x] The `restart` command accepts standard optional flags (e.g., `--force`, `--verbose`).
- [x] A stub `IServerProcessManager` is injected and invoked.

## Implementation Notes
- Created `RestartCommand.cs` extending `AsyncCommand<Settings>`.
- Registered the command via `CommandApp` in `Program.cs`.
- Restored `Cli/` directory to the `MininRouter.csproj` compilation scope.
- Updated Spectre.Console package and fixed legacy command `ExecuteAsync` signatures.
