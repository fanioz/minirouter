---
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["coderabbit", "dotnet-build"]
---

# Story 1.1: Scaffold Frontend Application and Configure CORS

## Status
Ready for Review

## Story
**As a** Developer,
**I want** to scaffold a new SPA and enable CORS on the backend,
**so that** the frontend can communicate with the MiniRouter API.

## Acceptance Criteria
1. A new SPA is generated in the `frontend/` directory.
2. `Program.cs` is updated to include a CORS policy allowing requests from the frontend development server.
3. Native AOT build still completes with zero trimming warnings.

## 🤖 CodeRabbit Integration

### Story Type Analysis
**Primary Type**: Frontend / Deployment
**Secondary Type(s)**: API
**Complexity**: Medium

### Specialized Agent Assignment
**Primary Agents**:
- @dev
- @github-devops

**Supporting Agents**:
- @architect

### Quality Gate Tasks
- [x] Pre-Commit (@dev): Run `coderabbit --prompt-only -t uncommitted` before marking story complete
- [ ] Pre-PR (@github-devops): Run `coderabbit --prompt-only --base main` before creating pull request
- [ ] Pre-Deployment (@github-devops): Run `coderabbit --prompt-only -t committed --base HEAD~10` before production deploy (if applicable)

### Self-Healing Configuration
**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix
- HIGH issues: document_only

### CodeRabbit Focus Areas
**Primary Focus**:
- CI/CD: Pipeline configuration, test coverage
- Native AOT compatibility of backend CORS changes

**Secondary Focus**:
- Responsive design: Mobile-first, breakpoints

## Tasks / Subtasks
- [x] Task 1 (AC: 1): Scaffold Frontend SPA
  - [x] Initialize a new SPA inside the `frontend/` directory.
  - [x] Add basic build scripts and configuration.
- [x] Task 2 (AC: 2): Configure CORS in Backend
  - [x] Update `Program.cs` to add CORS middleware.
  - [x] Allow origins for the frontend dev server (e.g., `http://localhost:5173` or `http://localhost:3000`).
- [x] Task 3 (AC: 3): Verify AOT Build
  - [x] Ensure backend CORS changes are AOT-compatible and do not produce trimming warnings.
  - [x] Run AOT publish command locally to verify zero trimming warnings.

## Dev Notes
- **Previous Story Insights**: N/A (First story of epic)
- **Data Models**: N/A
- **API Specifications**: The backend proxy must accept REST requests from the newly created frontend domain. [Source: docs/archive/system-architecture.md#High Level Architecture]
- **Component Specifications**: Baseline setup only. No complex components specified yet.
- **File Locations**: Create the SPA in `frontend/`. Update `Program.cs` in the root backend directory. [Source: docs/archive/system-architecture.md#Project Structure (Actual)]
- **Technical Constraints**: Any additions to `Program.cs` must remain Native AOT-safe. Do not introduce reflection-based configurations for CORS. [Source: docs/archive/system-architecture.md#Workarounds and Gotchas]
- **Testing Requirements**:
  - Integration Verification IV1: Verify that existing curl/Postman requests to the API still succeed.
  - Integration Verification IV2: Verify that the SPA can make a simple `GET /providers` request successfully.

### Testing
- Run `dotnet publish -c Release -r linux-x64 --self-contained -p:PublishAot=true` (or equivalent for your OS) to verify no trimming warnings.
- Run `dotnet run` and verify existing endpoints (`/health`, `/providers`) still work via curl.

## Change Log
| Date       | Version | Description     | Author       |
|------------|---------|-----------------|--------------|
| 2026-08-06 | 1.0     | Initial draft   | @sm (River)  |
| 2026-08-06 | 1.1     | Story completed | @dev (Dex)   |

## Dev Agent Record

### Agent Model Used
- Antigravity AutoClaude 3.0

### Debug Log References
- N/A

### Completion Notes List
- Scaffolded SPA with React, Vite, and Tailwind CSS v4 in `frontend/`.
- Updated `Program.cs` to add CORS middleware allowing requests from `http://localhost:5173` and `http://localhost:3000`.
- Verified local AOT build completed with zero trimming warnings.

### File List
- `Program.cs`
- `frontend/` (new directory)

## QA Results
