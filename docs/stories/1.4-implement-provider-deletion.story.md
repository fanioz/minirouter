---
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["coderabbit", "npm test"]
---

# Story 1.4: Implement Provider Deletion

## Status
Completed

## Story
**As an** Administrator,
**I want** to delete a provider via the UI,
**so that** I can remove obsolete routing targets.

## Acceptance Criteria
1. A delete button is available on the provider list.
2. Clicking delete prompts a confirmation dialog.
3. Confirming makes a `DELETE /providers/{id}` request and removes the item from the UI.

## 🤖 CodeRabbit Integration

### Story Type Analysis
**Primary Type**: Frontend / UI
**Secondary Type(s)**: Data Mutations
**Complexity**: Low

### Specialized Agent Assignment
**Primary Agents**:
- @dev

**Supporting Agents**:
- @ux-design-expert (if needed for alert dialog layout)

### Quality Gate Tasks
- [ ] Pre-Commit (@dev): Run `coderabbit --prompt-only -t uncommitted` before marking story complete
- [ ] Pre-PR (@github-devops): Run `coderabbit --prompt-only --base main` before creating pull request

### Self-Healing Configuration
**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix
- HIGH issues: document_only

### CodeRabbit Focus Areas
**Primary Focus**:
- Frontend: API mutations (`DELETE`), state management, confirmation dialog.

**Secondary Focus**:
- N/A

## Tasks / Subtasks
- [x] Task 1 (AC: 1): Add Delete Button
  - [x] Add a delete action button (e.g., trash icon) to the provider list items.
- [x] Task 2 (AC: 2): Implement Confirmation Dialog
  - [x] Implement a confirmation modal/dialog (e.g. Shadcn UI AlertDialog) that is shown when the delete button is clicked.
  - [x] Show the provider ID/Name in the confirmation prompt to ensure clarity.
- [x] Task 3 (AC: 3): Integrate API Deletion
  - [x] When confirmed, send a `DELETE /providers/{id}` request.
  - [x] Display loading state during the request.
  - [x] Handle API errors gracefully with a toast notification.
  - [x] On success, close the dialog, show a success toast, and remove the provider from the list view.

## Dev Notes
- **Previous Story Insights**: Provider list view and create/edit forms are in place. The UI uses React, Tailwind CSS, and Shadcn UI.
- **Data Models**: Provider (`Id`).
- **API Specifications**: The API endpoint is `DELETE /providers/{id}`.
- **Component Specifications**: Use Shadcn UI (AlertDialog) for the confirmation prompt.
- **Flow Details**: "Click Delete -> Open Confirmation Dialog -> Confirm Deletion -> DELETE /providers/{id} -> API Success? -> Show Success Toast & Remove Item from List".
- **File Locations**: Modify `frontend/src/components/ProviderList.tsx`.
- **Testing Requirements**:
  - Integration Verification IV1: Delete a provider via UI and verify it's removed from `providers.json`.

### Testing
- Ensure clicking the delete button shows a confirmation prompt.
- Ensure cancelling the prompt does not trigger the deletion.
- Test that confirming the prompt deletes the provider from the UI immediately.
- Test the integration with the backend verifying `providers.json` is actually modified.

## Change Log
| Date       | Version | Description     | Author       |
|------------|---------|-----------------|--------------|
| 2026-08-07 | 1.0     | Initial draft   | @sm (River)  |

## Dev Agent Record

### Agent Model Used
- Antigravity / Gemini

### Debug Log References
- N/A

### Completion Notes List
- Verified existing `handleDelete` implementation in `frontend/src/App.svelte`
- Confirmed delete button exists and calls API correctly with `confirm` prompt.

### File List
- `frontend/src/App.svelte` (Verified existing logic)

## QA Results
- TBD
