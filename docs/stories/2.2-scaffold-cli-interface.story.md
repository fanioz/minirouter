---
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["coderabbit", "dotnet-build"]
---

# Story 2.2: Scaffold CLI Interface

## Status
Done

## Story
**As a** Developer,
**I want** to build a robust CLI for the Agentic Interface using Spectre.Console,
**so that** users and agents can interact with the MiniRouter via command line.

## Acceptance Criteria
1. A CLI project or `Cli/` folder is set up for the Agentic Interface.
2. The Spectre.Console package is referenced and integrated.
3. A basic `minirouter providers list` command is built and functional.

## 🤖 CodeRabbit Integration

### Story Type Analysis
**Primary Type**: CLI / Tooling
**Secondary Type(s)**: UI
**Complexity**: Medium

### Specialized Agent Assignment
**Primary Agents**:
- @dev

**Supporting Agents**:
- @architect

### Quality Gate Tasks
- [x] Pre-Commit (@dev): Run `coderabbit --prompt-only -t uncommitted` before marking story complete
- [ ] Pre-PR (@github-devops): Run `coderabbit --prompt-only --base main` before creating pull request

### Self-Healing Configuration
**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix
- HIGH issues: document_only

### CodeRabbit Focus Areas
**Primary Focus**:
- Spectre.Console integration and CLI architecture
- Command routing and argument parsing

**Secondary Focus**:
- UX of the CLI output

## Tasks / Subtasks
- [x] Task 1 (AC: 1): Setup CLI Environment
  - [x] Create a `Cli/` folder or a new CLI project depending on architecture alignment.
  - [x] Add necessary project references to access core MiniRouter services.
- [x] Task 2 (AC: 2): Integrate Spectre.Console
  - [x] Add the `Spectre.Console` and `Spectre.Console.Cli` NuGet packages.
  - [x] Set up the basic CLI application runner in the entry point.
- [x] Task 3 (AC: 3): Build Providers List Command
  - [x] Create a command class for `minirouter providers list`.
  - [x] Implement the command logic to fetch and display providers using Spectre.Console tables or trees.

## Dev Notes
- **Previous Story Insights**: We are adding a CLI interface alongside the Razor Pages dashboard.
- **Data Models**: Use existing Provider models.
- **API Specifications**: N/A
- **Component Specifications**: Use Spectre.Console for CLI UX.
- **File Locations**: CLI code should reside in a `Cli/` directory or project.
- **Technical Constraints**: N/A
- **Testing Requirements**:
  - Integration Verification IV1: Verify that running the CLI command `minirouter providers list` outputs the providers correctly.

### Testing
- Run `dotnet build` and ensure there are no errors.
- Run the CLI application locally with the `providers list` arguments and verify the output.

## Change Log
| Date       | Version | Description     | Author       |
|------------|---------|-----------------|--------------|
| 2026-08-06 | 1.0     | Initial draft   | @sm (River)  |
| 2026-08-06 | 1.1     | Implementation  | @dev (Dex)   |
| 2026-08-06 | 1.2     | QA Review Pass  | @qa (Quinn)  |

## Dev Agent Record

### Agent Model Used
- @dev (Antigravity)

### Debug Log References
- N/A

### Completion Notes List
- Scaffolded `Cli/` directory with `Infrastructure` and `Commands` folders.
- Implemented `TypeRegistrar` and `TypeResolver` for `Spectre.Console.Cli` DI integration.
- Configured `Program.cs` to intercept CLI arguments and route to `CommandApp` using the existing WebApplication setup's services (like `ProviderService`).
- Created `ProvidersListCommand` to fetch and list providers using `Spectre.Console` Table.
- Tested and verified CLI command works effectively (`dotnet run -- providers list`).

### File List
- `MininRouter.csproj` (Modified)
- `Program.cs` (Modified)
- `Cli/Infrastructure/TypeRegistrar.cs` (New)
- `Cli/Infrastructure/TypeResolver.cs` (New)
- `Cli/Commands/ProvidersListCommand.cs` (New)

## QA Results
- **Gate Decision:** PASS
- **Reviewer:** @qa (Quinn)
- **Date:** 2026-08-06
- **Notes:** Spectre.Console.Cli configured perfectly with proper DI isolation. `providers list` command executes locally with correct output formatting. See `docs/reviews/qa-review-phase-2.md` for full report.
