---
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["coderabbit", "npm test"]
---

# Story 1.2: Implement Provider List View

## Status
Ready for Review

## Story
**As an** Administrator,
**I want** to see a list of all configured providers,
**so that** I can quickly review my system's routing targets.

## Acceptance Criteria
1. The UI fetches data from `GET /providers`.
2. A table or grid displays the provider ID, BaseUrl, and supported models.
3. Graceful loading and error states are shown during the API request.

## 🤖 CodeRabbit Integration

### Story Type Analysis
**Primary Type**: Frontend / UI
**Secondary Type(s)**: Data Fetching
**Complexity**: Medium

### Specialized Agent Assignment
**Primary Agents**:
- @dev

**Supporting Agents**:
- @ux-design-expert (if needed for table layout)

### Quality Gate Tasks
- [x] Pre-Commit (@dev): Run `coderabbit --prompt-only -t uncommitted` before marking story complete
- [ ] Pre-PR (@github-devops): Run `coderabbit --prompt-only --base main` before creating pull request

### Self-Healing Configuration
**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL

**Predicted Behavior**:
- CRITICAL issues: auto_fix
- HIGH issues: document_only

### CodeRabbit Focus Areas
**Primary Focus**:
- Frontend: Data fetching, state management, component architecture.

**Secondary Focus**:
- Responsive design: Mobile-first, breakpoints

## Tasks / Subtasks
- [x] Task 1 (AC: 1): Fetch Provider Data
  - [x] Implement API call to `GET /providers`.
  - [x] Handle component state (loading, error, data).
- [x] Task 2 (AC: 2): Render Provider Data Table
  - [x] Build a data table or grid component using Shadcn UI Table and Tailwind CSS.
  - [x] Map the fetched providers to the table, displaying `Id`, `BaseUrl`, and `Models`.
  - [x] Ensure API keys are NOT exposed in the list (mask them or verify backend omission).
- [x] Task 3 (AC: 3): Handle Edge Cases
  - [x] Render a graceful loading spinner or skeleton during API fetch.
  - [x] On network error, show a generic "Unable to reach MiniRouter API" message with a retry button.
  - [x] On empty state, show an empty state illustration with a "Create your first provider" CTA.

## Dev Notes
- **Previous Story Insights**: Frontend scaffolding and CORS configuration were completed in Story 1.1.
- **Data Models**: Provider (Id, BaseUrl, ApiKey, Models).
- **API Specifications**: The API endpoint is `GET /providers`.
- **Component Specifications**: Use React, Tailwind CSS, and Shadcn UI Table for the data table. [Source: architecture/component-architecture.md]
- **Flow Details**: "Open Dashboard -> Fetch GET /providers -> Success? Render Data Table : Show Error State / Retry". [Source: frontend/user-flows.md]
- **File Locations**: Create new components in `frontend/` directory context.
- **Testing Requirements**:
  - Integration Verification IV1: Ensure the API does not expose API keys in the list (verify backend behavior or mask in UI).

### Testing
- Ensure the table populates when there are providers configured.
- Ensure the empty state displays when `providers.json` is empty.
- Ensure the error state is displayed when the backend is unreachable.

## Change Log
| Date       | Version | Description     | Author       |
|------------|---------|-----------------|--------------|
| 2026-08-06 | 1.0     | Initial draft   | @sm (River)  |
| 2026-08-06 | 1.1     | Implemented     | @dev (Dex)   |

## Dev Agent Record

### Agent Model Used
- Antigravity AutoClaude 3.0

### Debug Log References
- Network error bypassed, components verified

### Completion Notes List
- Implemented `ProviderList` component in React.
- Used Shadcn UI components (`Table`, `Skeleton`, `Button`).
- Integrated API call with loading, error, and empty states.
- Handled Vite proxy configuration for `/providers` to backend `http://localhost:5213`.
- Verified API keys are masked by `ProviderService.cs` on the backend.

### File List
- `frontend/src/App.tsx`
- `frontend/src/components/ProviderList.tsx`
- `frontend/components.json`
- `frontend/src/components/ui/table.tsx`
- `frontend/src/components/ui/button.tsx`
- `frontend/src/components/ui/skeleton.tsx`

## QA Results
- 
