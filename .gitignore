# Build outputs
bin/
obj/
publish/
publish-osx/

# Local config with real API keys
providers.json

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Environment & Secrets (AIOX)
.env
.env.local
.env.*.local
*.key
*.pem

# Dependencies (AIOX)
node_modules/
node_modules

# Build & Logs (AIOX)
dist/
build/
*.log
logs/

# IDE & OS (AIOX)
Thumbs.db
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# Docs
docs/reff

# Runtime
*.pid
*.db
test_*.json


# Runtime artifacts (added by cleanup)
aot_run.log
server.log
server2.log
test1_out.txt
test2_out.txt
minirouter.db
.minirouter.pid
REPOLORE.md
providers.json.backup
.aiox/

# Backup file berisi API key asli — jangan pernah commit
providers.json.bak
providers.json.*

# ── Public repo cleanup (2026-08-28) ──
# Agent/tooling configs — lokal saja, tidak untuk publik
.aiox-core/
.aiox/
.claude/
.cursor/
.gemini/
.codex/
.kimi/
.antigravity/
.agent/
.github/
AGENTS.md

# Dead code — Razor spike, di-exclude dari build
Pages/

# Build output — diregenerasi oleh hook BeforeBuild (npm run build)
wwwroot/

# Machine caches di dalam design package
frontend/design/.file-versions/
frontend/design/.omo/
